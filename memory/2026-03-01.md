# 2026-03-01 — Daily Notes

## System Optimization Session
Major performance tuning done today:

### Fixes Applied
1. **Memory search fixed** — Google embedding provider had `baseUrl: "https://unused"`, changed to `https://generativelanguage.googleapis.com/v1beta`
2. **Workspace context trimmed 56%** — 47.6KB → 21.1KB
   - MEMORY.md: 17.7KB → 6KB (archived completed projects, removed duplicates)
   - AGENTS.md: 16.4KB → 3.1KB (removed duplication with SOUL.md/USER.md/MEMORY.md)
   - USER.md: 2.5KB → 0.9KB (consolidated report standards)
3. **Stale sessions cleaned** — ran `sessions cleanup --enforce --all-agents`
4. **2 failing cron jobs fixed:**
   - `agent-work-logger` — recreated, writes to `/workspace/tmp/` instead of vault (sandbox can't access vault)
   - `fb-ai-news-post` — recreated, writes to `/workspace/tmp/` instead of `/tmp/` (sandbox restriction)
5. **BOT_COMMANDS_TOO_MUCH fixed** — disabled native Telegram command registration (`channels.telegram.commands.native: false`)
6. **GOG env vars added to systemd** — `GOG_KEYRING_PASSWORD` + `GOG_ACCOUNT` now available to heartbeats/cron
7. **17 old memory files archived** to `memory/archive/` (pre-Feb 15)
8. **HEARTBEAT.md rewritten** — rotation-based checks, strict silent rules

### Gateway Restarts
- Multiple restarts due to `doctor fix` cascade + config patches
- One restart timed out (60s health check timeout)
- All stable after final restart ~22:09 Bangkok

### Key Learnings
- Google embedding baseUrl was set to "https://unused" — likely from a config wizard placeholder
- Sandbox can't write to `/tmp/` OR `~/obsidian-vault/` — both cron jobs failed for this reason
- Provider timeouts not configurable at provider level in v2026.2.26
- QMD backend needs bun+qmd CLI installed — skipped for now
- OpenClaw v2026.2.26 is latest available


---
## /remember — 2026-03-01 23:00 BKK


### ✅ Decisions Made
- "https://unused" — likely from a config wizard placeholder

### 📌 Facts to Remember
- "https://unused"`, changed to `https://generativelanguage.googleapis.com/v1beta`
- was set to "https://unused" — likely from a config wizard placeholder
- — skipped for now
